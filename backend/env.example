# Database
DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/dengue_surveillance?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars-long
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Optional additional frontend URLs (comma-separated) for CORS (e.g. admin + public forecast)
# FRONTEND_URLS=https://your-admin.vercel.app,https://your-forecast.vercel.app

